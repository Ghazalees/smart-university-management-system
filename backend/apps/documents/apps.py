from django.apps import AppConfig


class DocumentsConfig(AppConfig):
    """Configuration for the document and knowledge-base metadata app."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.documents"
